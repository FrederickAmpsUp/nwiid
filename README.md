# `nwiid` Wii Remote Interface

## What's this?
`nwiid` is a modernization of [`abstrakraft/cwiid`](https://github.com/abstrakraft/cwiid) using CMake.

## Building
```
cmake -B build
cmake --build build
```
